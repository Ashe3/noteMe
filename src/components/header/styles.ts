import { css } from '@emotion/react';

export default css`
  width: 100vw;
  min-height: 60px;

  color: #374046;
  padding: 0 30px;

  box-shadow: 0px 4px 9px 0px rgba(0, 0, 0, 0.15);
  display: flex;
  align-items: center;
  position: relative;
`;
